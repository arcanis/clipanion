import {lazy}    from "../../sources/advanced/lazy";
import {Command} from "../../sources/advanced";

describe(`Lazy commands`, () => {
  it(`should lazy-load commands based on the arguments`, () => {
    const spy = jest.fn();

    //lazy([`foo`, `bar`], async segments => {
    //});
  });
});
